package com.fantasysport.utility;

/**
 * Created by bylynka on 5/15/14.
 */
public class BsonToJsonConverter {

    public static String convert(String bson){
        int length = bson.length();
        int klength = (int)bson.charAt(0);

        for (int i = 0; i < 10; i++){

        }
//        for (int length = bson.length,
//klength = hlist[0],
//                     result = Array(((length - klength - 1) / klength) || 0),
//                     i = 1 + klength,
//                     j = 0,
//                     ki, o;
//             i < length;
//                ) {
//            for (
//                    result[j++] = (o = {}), ki = 0;
//                    ki < klength;
//                    o[hlist[++ki]] = hlist[i++]
//                    );
//        }
//        return result;

    }
}
